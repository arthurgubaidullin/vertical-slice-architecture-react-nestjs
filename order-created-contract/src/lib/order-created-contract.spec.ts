import { orderCreatedContract } from './order-created-contract';

describe('orderCreatedContract', () => {
  it('should work', () => {
    expect(orderCreatedContract()).toEqual('order-created-contract');
  });
});
